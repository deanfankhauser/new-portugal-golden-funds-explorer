name: Generate Redirects

on:
  workflow_dispatch: # Manual trigger
  push:
    branches: [main, develop]
    paths:
      - 'src/data/services/tags-service.ts'
      - 'src/data/services/categories-service.ts'
      - 'scripts/ssg/generate-redirects.ts'

jobs:
  generate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Generate redirects
        run: npm run generate:redirects
      
      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet vercel.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add vercel.json
          git commit -m "chore: auto-generate redirects [skip ci]"
          git push
